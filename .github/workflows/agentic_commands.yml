# gh-aw-commands: {"payload_version":"v1","schema_version":"v1","compiler_version":"v0.86.2","commands":["review"],"workflows":["aspnetcore-pr-review"]}
# Routing summary (sorted):
#   slash commands:
#     /review -> aspnetcore-pr-review [pull_request_comment,pull_request_review_comment]
#   labels:
#     (none)
# This file was automatically generated by gh-aw (v0.86.2). DO NOT EDIT. To debug this workflow, load the skill at https://github.com/github/gh-aw/blob/main/debug.md
#
#    ___                   _   _
#   / _ \                 | | (_)
#  | |_| | __ _  ___ _ __ | |_ _  ___
#  |  _  |/ _` |/ _ \ '_ \| __| |/ __|
#  | | | | (_| |  __/ | | | |_| | (__
#  \_| |_/\__, |\___|_| |_|\__|_|\___|
#          __/ |
#  _    _ |___/
# | |  | |                / _| |
# | |  | | ___ _ __ _  __| |_| | _____      ____
# | |/\| |/ _ \ '__| |/ /|  _| |/ _ \ \ /\ / / ___|
# \  /\  / (_) | | | | ( | | | | (_) \ V  V /\__ \
#  \/  \/ \___/|_| |_|\_\|_| |_|\___/ \_/\_/ |___/
#
#
# To regenerate this workflow, run:
#   gh aw compile
# Not all edits will cause changes to this file.
#
# For more information: https://github.github.com/gh-aw/introduction/overview/
#
name: "Agentic Commands"

on:
  issue_comment:
    types: [created, edited]
  pull_request_review_comment:
    types: [created, edited]

permissions: {}

jobs:
  route:
    runs-on: ubuntu-slim
    timeout-minutes: 15
    permissions:
      actions: write
      contents: read
      issues: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Scripts
        uses: github/gh-aw-actions/setup@v0.86.2
        with:
          destination: ${{ runner.temp }}/gh-aw/actions

      - name: Route slash command
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
        env:
          GH_AW_SLASH_ROUTING: '{"review":[{"workflow":"aspnetcore-pr-review","events":["pull_request_comment","pull_request_review_comment"]}]}'
          GH_AW_LABEL_ROUTING: '{}'
          GH_AW_HELP_COMMANDS: '[{"command":"review","description":"Maintainer-invoked, read-only domain-expert review of a pull request. A maintainer types `/review` in a pull request comment or review comment; the agent freezes the pull request head commit, reads the GitHub-authoritative diff, routes the changed paths to the matching domain reviewers plus always-on cross-cutting review, and posts at most five inline review comments plus a single COMMENT-only review. It never approves, never requests changes, never checks out or runs pull request code, and never mutates anything else.","centralized":true,"decentralized":false,"source_file":"aspnetcore-pr-review"}]'
          GH_AW_HELP_COMMAND_ENABLED: 'true'
          GH_AW_SLASH_COMMAND_DOCS_URL: 'https://github.github.com/gh-aw/reference/command-triggers/'
        with:
          script: |
            const { setupGlobals } = require('${{ runner.temp }}/gh-aw/actions/setup_globals.cjs');
            setupGlobals(core, github, context, exec, io, getOctokit);
            const { main } = require('${{ runner.temp }}/gh-aw/actions/route_slash_command.cjs');
            await main();
